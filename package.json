{
  "name": "wgsleng",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "npx -y live-server",
    "native": "cd native && cargo build --release",
    "game:bob": "rm -f bob.zip && cd examples/bob && zip ../../bob.zip *.wgsl *.png *.ogg",
    "game:snake": "rm -f snake.zip && cd examples/snake && zip ../../snake.zip *.wgsl"
  },
  "author": "konsumer",
  "type": "module"
}
